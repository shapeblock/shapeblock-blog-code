#!/bin/bash -e
cat <<EOF
This is the Ubuntu LEMP S2I image from the shapeblock.com blog:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> ubuntu-lemp-s2i <application image>

You can then run the resulting image via:
docker run <application image>
EOF
